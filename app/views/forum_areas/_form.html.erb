<%= form_for(@forum_area) do |f| %>
  <% if @forum_area.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@forum_area.errors.count, "error") %> prohibited this forum_area from being saved:</h2>

      <ul>
      <% @forum_area.errors.full_messages.each do |msg| %>
        <li><%= msg %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= f.label :title %><br />
    <%= f.text_field :title %>
  </div>
  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
